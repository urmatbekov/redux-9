const addCount = () => {
    return { type: "add" }
}
const minusCount = () => {
    return { type: "minus" }
}
const multiplyCount = () => {
    return { type: "multiply" }
}
const devideCount = () => {
    return { type: "devide" }
}

export { addCount, minusCount, multiplyCount, devideCount }